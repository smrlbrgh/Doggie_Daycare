<h1>Listing dogs</h1>

<table class= "table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Owner</th>
      <th>Breed</th>
      <th>Dob</th>
      <th>Vet</th>
      <th>Vet phone</th>
      <th>In daycare</th>
      <th>Show</th>
      <th>Edit</th>
      <th>Delete</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dogs.each do |dog| %>
      <tr>
        <td><%= dog.name %></td>
        <td><%= dog.owner.last_name %></td>
        <td><%= dog.breed.breed %></td>
        <td><%= dog.dob %></td>
        <td><%= dog.vet %></td>
        <td><%= dog.vet_phone %></td>
        <td><%= dog.in_daycare %></td>
        <td><%= link_to ' ', dog, class: "fa fa-picture-o" %></td>
        <td><%= link_to ' ', edit_dog_path(dog), class: "fa fa-pencil-square-o" %></td>
        <td><%= link_to ' ', dog, method: :delete, data: { confirm: 'Are you sure?' }, class: "fa fa-trash-o" %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dog', new_dog_path, class: "btn btn-success btn-xs" %>
